apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-helm-argocd.fullname" . }}
  labels:
    {{- include "project-helm-argocd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
      name: http
  selector:
    {{- include "project-helm-argocd.selectorLabels" . | nindent 4 }}
